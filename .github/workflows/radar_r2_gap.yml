name: Radar R2 FINCO GAP

on:
  push:
    branches: [radar/r2-finco-gap]
    paths:
      - "finco_radar/gap/**"
      - "finco_radar/r2/**"
      - "tests/test_radar_r2_gap.py"
      - "docs/radar/r2_finco_gap.md"
      - ".github/workflows/radar_r2_gap.yml"
  workflow_dispatch:

jobs:
  finco-gap:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: Install Radar test/runtime dependencies
        run: python -m pip install "httpx>=0.27,<1" "pytest>=8,<10"
      - name: Run focused R2 tests
        run: pytest -q tests/test_radar_r2_gap.py
      - name: Run R2 live FINCO GAP proof
        run: python -m finco_radar.r2.live_proof
      - name: Require directional gap invariants
        run: |
          python - <<'PY'
          import json
          from decimal import Decimal
          from pathlib import Path
          data = json.loads(Path("artifacts/radar_r2_finco_gap_evidence.json").read_text())
          if data["status"] != "PASS":
              raise SystemExit("R2_BLOCKED: live proof not PASS")
          key = str(data["asset"]["canonicalKey"])
          if not key.startswith("4663:0x"):
              raise SystemExit("R2_BLOCKED: selected asset is not a canonical Robinhood Chain key")
          if data["reference"]["currency"] != "USD":
              raise SystemExit("R2_BLOCKED: non-USD reference reached FINCO GAP")
          if data["referenceStateAuthority"] != "R4_NOT_YET_APPLIED":
              raise SystemExit("R2_BLOCKED: R2 falsely claimed reference-state authority")
          observations = data["observations"]
          if len(observations) != 4:
              raise SystemExit("R2_BLOCKED: expected four size/side observations")
          expected = {("BUY", "100"), ("BUY", "1000"), ("SELL", "100"), ("SELL", "1000")}
          actual = {(item["side"], item["requestedNotionalUsd"]) for item in observations}
          if actual != expected:
              raise SystemExit(f"R2_BLOCKED: observation matrix mismatch {actual}")
          for item in observations:
              if item["side"] == "BUY" and item["referenceSide"] != "ASK":
                  raise SystemExit("R2_BLOCKED: BUY did not use ASK")
              if item["side"] == "SELL" and item["referenceSide"] != "BID":
                  raise SystemExit("R2_BLOCKED: SELL did not use BID")
              if item["costScope"] != "ROUTE_AMOUNTS_ONLY_FEES_AND_GAS_NOT_ADDED":
                  raise SystemExit("R2_BLOCKED: cost scope changed")
              if item["referenceStateAuthority"] != "R4_NOT_YET_APPLIED":
                  raise SystemExit("R2_BLOCKED: observation claimed R4 authority")
              values = [
                  Decimal(item["executionPriceUsdPerToken"]),
                  Decimal(item["referencePriceUsdPerToken"]),
                  Decimal(item["gapBps"]),
              ]
              if not all(value.is_finite() for value in values):
                  raise SystemExit("R2_BLOCKED: non-finite numeric observation")
              if values[0] <= 0 or values[1] <= 0:
                  raise SystemExit("R2_BLOCKED: non-positive execution/reference price")
          print(f"R2_FINCO_GAP_PASS key={key} observations={len(observations)}")
          PY
      - name: Run R1 canonical registry regression
        env:
          RADAR_R1_EVIDENCE_PATH: artifacts/radar_r1_regression_on_r2.json
        run: python -m finco_radar.r1.live_proof
      - name: Run R0 execution quote regression
        env:
          RADAR_R0_EVIDENCE_PATH: artifacts/radar_r0_regression_on_r2.json
        run: python -m finco_radar.r0.live_proof
      - name: Require original R0 size-aware acceptance
        run: |
          python - <<'PY'
          import json
          from decimal import Decimal
          from pathlib import Path
          data = json.loads(Path("artifacts/radar_r0_regression_on_r2.json").read_text())
          if data["status"] != "PASS":
              raise SystemExit("R0_REGRESSION_BLOCKED: live proof not PASS")
          buy = Decimal(data["sizeImpactBps"]["BUY"])
          sell = Decimal(data["sizeImpactBps"]["SELL"])
          if buy == 0 and sell == 0:
              raise SystemExit("R0_REGRESSION_BLOCKED: no size-dependent quote economics observed")
          print(f"R0_SIZE_AWARE_REGRESSION_PASS BUY={buy}bps SELL={sell}bps")
          PY
      - name: Upload R2 evidence
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: radar-r2-finco-gap-evidence
          path: |
            artifacts/radar_r2_finco_gap_evidence.json
            artifacts/radar_r1_regression_on_r2.json
            artifacts/radar_r0_regression_on_r2.json
          if-no-files-found: error
